ParaView CoProcessing version of:
https://math.lsi.umich.edu/ParallelSpectral/CubicNonlinearSchrodinger/Programs/NLS3dFortranMPI

Uses the paraview/3.14.1 module.
Rename one of pipeline_* to pipeline.py to run. Pipeline file needs to be in the same directory as the executable.

pipeline_vtis.py outputs VTK XML Image Data files. These are RAW binary files in an XML wrapper. pipline_images.py outputs .pngs.

Output interpreted as cellular data by coprocessor.
